language: java

jdk:
  - oraclejdk7
  - openjdk7
  - openjdk6

branches:
  except:
    - /^v|d\d+\.\d+\.\d+$/

before_install:
  - sudo apt-get install -y realpath

script:
  - ./jhove-bbt/scripts/travis-test.sh

after_success:
  - bash <(curl -s https://codecov.io/bash)
